package backend.academy.hangman.game.state;

import backend.academy.hangman.game.state.vital.GameStatus;

public final class MenuState extends State {
    public MenuState(GameStatus status) {
        super(status);
    }
}
